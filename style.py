# -*- coding: utf-8 -*-
#!/usr/bin/env python

import os, sys

l_fd = []
if len(sys.argv) == 1:
    print("Invalide Argument")
    exit()
for i in range(1, len(sys.argv)-1):
    try:
        l_fd.append(open())
